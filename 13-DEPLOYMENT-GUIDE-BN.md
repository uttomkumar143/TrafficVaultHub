# TrafficVaultHub — ডিপ্লয়মেন্ট ও সেটআপ গাইড (বাংলা)

এই গাইডটা **আপনার (মানুষ) জন্য** — genspark.com, GitHub, আর Cloudflare একসাথে কানেক্ট করে কীভাবে পুরো সিস্টেমটা চালু করবেন সেটা ধাপে ধাপে বলা আছে। genspark বা Cloudflare-এর মেনু/বাটনের নাম সময়ে সময়ে সামান্য বদলাতে পারে — তাই এখানে বলা প্রতিটা ধাপের **উদ্দেশ্য** বোঝাটাই আসল, ঠিক নামটা না মিললে সেই নামের কাছাকাছি অপশন খুঁজে নেবেন।

---

## ধাপ ১ — GitHub রিপো বানানো

1. GitHub-এ লগইন করুন, নতুন **প্রাইভেট** রিপোজিটরি বানান — নাম দিন `trafficvaulthub`।
2. এই কিটের `docs/PRD.md` ফাইলটা রিপোতে `docs/PRD.md` পাথে আপলোড করে দিন।
3. `STATE-TEMPLATE.md` ফাইলটা রিপোর রুটে আপলোড করুন এবং নাম বদলে `STATE.md` করে দিন।
4. প্রথম commit করে দিন — এখন রিপোতে শুধু PRD আর STATE.md আছে, যথেষ্ট শুরুর জন্য।

## ধাপ ২ — genspark.com-এ Claude Fable 5.1 চালু করা

1. genspark.com-এ অ্যাকাউন্ট খুলুন / লগইন করুন।
2. এমন একটা ওয়ার্কস্পেস বা এজেন্ট মোড বাছাই করুন যেখানে **কোডিং/ডেভেলপার এজেন্ট** ব্যবহার করা যায় এবং মডেল হিসেবে **Claude Fable 5.1**-কে বেছে নেওয়ার অপশন আছে (মডেল সিলেক্টর/সেটিংসে খুঁজবেন)।
3. ফ্রি ট্রায়াল কতটুকু ব্যবহার হয়েছে/বাকি আছে — সেটা genspark-এর নিজের বিলিং বা ইউসেজ পেজে দেখা যাবে। এই সংখ্যাটা genspark ও Anthropic নিজেরাই নিয়ন্ত্রণ করে, এই কিট শুধু আপনার **কাজ যেন হারিয়ে না যায়** তা নিশ্চিত করে।

## ধাপ ৩ — GitHub কানেক্ট করা

1. genspark-এর সেটিংস/ইন্টিগ্রেশনস অংশে "Connect GitHub" বা এই জাতীয় অপশন খুঁজুন।
2. GitHub OAuth অনুমোদন দিন এবং শুধুমাত্র `trafficvaulthub` রিপোর অ্যাক্সেস দিন (পুরো অ্যাকাউন্ট-ওয়াইড অ্যাক্সেস না দেওয়াই ভালো, যদি genspark সেই অপশন দেয়)।
3. চ্যাট/এজেন্ট সেশনে সেই রিপোটাকে "active repo" হিসেবে সিলেক্ট করুন, যাতে Claude সরাসরি রিপোতে read/write/commit/push করতে পারে।
4. একবার একটা ছোট টেস্ট চালান: এজেন্টকে বলুন "README.md ফাইলে এক লাইন যোগ করে commit ও push করো" — যদি সেটা GitHub-এ সত্যিই দেখা যায়, তাহলে কানেকশন ঠিক আছে।

> **গুরুত্বপূর্ণ:** এই টেস্টটা পাস হওয়ার আগে বড় কোনো প্রম্পট (মাস্টার প্রম্পট/ফেজ প্রম্পট) দেবেন না — আগে নিশ্চিত হোন commit+push আসলেই কাজ করছে।

## ধাপ ৪ — Cloudflare অ্যাকাউন্ট ও ক্রেডেনশিয়াল রেডি করা

TrafficVaultHub পুরোপুরি Cloudflare-এর উপর বানানো (Workers, D1, KV, R2, Queues, Pages, Durable Objects)। তাই লাগবে:

1. **Cloudflare অ্যাকাউন্ট** — cloudflare.com-এ ফ্রি অ্যাকাউন্ট যথেষ্ট শুরুর জন্য।
2. **Cloudflare API Token** — Cloudflare Dashboard → My Profile → API Tokens → "Create Token" — Workers, Pages, D1, KV, R2, Queues এডিট করার পারমিশনসহ একটা টোকেন বানান।
3. **Account ID** — Cloudflare Dashboard-এর ডানপাশে/ওভারভিউ পেজে পাওয়া যাবে।
4. **D1 ডাটাবেস, KV namespace, R2 bucket তৈরি** — Cloudflare Dashboard থেকে অথবা `wrangler` CLI দিয়ে (এজেন্ট আপনাকে ঠিক কমান্ড বলে দেবে Phase 0/Phase 9-এ)।

### এই ক্রেডেনশিয়ালগুলো কোথায় রাখবেন — এবং কোথায় রাখবেন **না**

- ❌ **কখনোই** genspark চ্যাটে বা কোনো প্রম্পট ফাইলে API Token/সিক্রেট পেস্ট করবেন না।
- ✅ genspark-এর "Secrets" / "Environment Variables" সেকশনে দিন (যদি এজেন্ট সরাসরি deploy করার জন্য এগুলো লাগে)।
- ✅ GitHub রিপোর **Settings → Secrets and variables → Actions**-এ দিন — নাম দিন `CLOUDFLARE_API_TOKEN` এবং `CLOUDFLARE_ACCOUNT_ID`। Phase 9-এর প্রম্পট এই নামগুলোই খুঁজবে GitHub Actions workflow-এ।
- ✅ কোডে শুধু `.env.example` রাখুন placeholder ভ্যালু দিয়ে, আসল ভ্যালু কখনো commit হবে না (`.gitignore`-এ `.env` বাদ দেওয়া আছে — Phase 0-তেই এটা করা হবে)।

## ধাপ ৫ — বিল্ড প্রসেস শুরু করা

`00-README-প্রথমে-এটা-পড়ুন.md`-এ বলা লুপ অনুযায়ী কাজ করুন:

1. প্রতিটা নতুন সেশনে প্রথমে `01-MASTER-SYSTEM-PROMPT.md` পেস্ট করুন।
2. তারপর `02-AUTO-COMMIT-PROTOCOL.md` পেস্ট করুন।
3. এজেন্টকে PRD.md ও STATE.md পড়ে বর্তমান ধাপ নিশ্চিত করতে বলুন।
4. তারপর যে ফেজে আছেন তার প্রম্পট (`03` থেকে `12` পর্যন্ত) পেস্ট করুন এবং কাজ করতে দিন।
5. genspark GitHub-এ কানেক্টেড থাকলে, এজেন্ট নিজে থেকেই ছোট ছোট কাজ শেষে commit + push করবে (এটাই `02` ফাইলের নিয়ম)। আপনি চাইলে মাঝে মাঝে GitHub রিপোতে গিয়ে commit history দেখে নিশ্চিত হতে পারেন যে সত্যিই push হচ্ছে।

## ধাপ ৬ — লিমিট শেষ হয়ে গেলে কী করবেন

- চিন্তার কিছু নেই — GitHub-এ সর্বশেষ commit পর্যন্ত সব কাজ সেভ আছে।
- নতুন সেশন খুলুন (নতুন ফ্রি ট্রায়াল/quota reset হলে, বা অন্য কোনো Claude অ্যাক্সেস দিয়ে হলেও চলবে — যেহেতু সব কনটেক্সট রিপোতেই আছে, চ্যাট-মেমোরির উপর নির্ভর করতে হচ্ছে না)।
- ধাপ ৫ থেকে আবার শুরু করুন। STATE.md এজেন্টকে বলে দেবে ঠিক কোথায় কাজ থেমেছিল।

## ধাপ ৭ — শেষে প্রোডাকশনে ডিপ্লয় করা (Phase 9 শেষ হওয়ার পর)

1. GitHub Actions-এর Secrets ঠিকমতো সেট করা আছে কিনা আরেকবার নিশ্চিত করুন (ধাপ ৪ দেখুন)।
2. Phase 9-এর প্রম্পট অনুযায়ী তৈরি হওয়া `.github/workflows/deploy.yml` অনুযায়ী রিলিজ ব্র্যাঞ্চে push দিলেই deploy শুরু হবে।
3. deploy শেষে `docs/runbooks/deployment.md` আর `docs/runbooks/rollback.md` পড়ে রাখুন — কিছু ভুল হলে কীভাবে rollback করবেন তা সেখানে লেখা থাকবে।

---

## সংক্ষেপে চেকলিস্ট

- [ ] GitHub প্রাইভেট রিপো বানানো হয়েছে, PRD.md ও STATE.md আপলোড করা হয়েছে
- [ ] genspark-এ Claude Fable 5.1 সিলেক্ট করা হয়েছে
- [ ] genspark-এ GitHub কানেক্ট করা হয়েছে এবং টেস্ট commit সফল হয়েছে
- [ ] Cloudflare অ্যাকাউন্ট, API Token, Account ID রেডি
- [ ] API Token শুধু GitHub Secrets / genspark Secrets-এ আছে, কোথাও চ্যাটে বা কোডে পেস্ট করা হয়নি
- [ ] Phase 0 থেকে শুরু করে ধাপে ধাপে এগোনো হচ্ছে, প্রতি সেশনে মাস্টার প্রম্পট + অটো-কমিট প্রোটোকল আগে পেস্ট করা হচ্ছে
